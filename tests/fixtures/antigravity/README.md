The transcript fixture uses the captured Antigravity 1.1.14 record shapes
reported in https://jazzyalex.github.io/agent-sessions/guides/antigravity-cli-local-history.html.
Paths and content are illustrative, not a copied private session. Each planner
call is followed by its typed result; the grep result preserves the documented
`truncated_fields` envelope.
